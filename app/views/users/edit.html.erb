<h1>Editing User</h1>
<%= form_for @user, url: {action: 'update'}, html: {class: 'form_update_user'} do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email %>
  </div>

  <div class="field">
    <%= label_tag :name %>
    <%= text_field :patient, :name %>
  </div>

  <div class="field">
    <%= label_tag :allergy %>
    <%= text_field :patient, :allergy %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
